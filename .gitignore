# Dependencies
node_modules/
package-lock.json  # (선택 사항: 보통은 커밋하지만, 팀/상황에 따라 제외하기도 함. 일반적으론 커밋 추천)
yarn.lock          # (선택 사항: 위와 동일)

# Environment variables (중요: 보안을 위해 절대 올리면 안 됨)
.env
.env.local
.env.*.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pids
*.pid
*.seed
*.pid.lock

# Builds (TypeScript 등을 사용해 빌드하는 경우)
dist/
build/

# Editor & OS files
.DS_Store
.vscode/
.idea/
*.swp

# Testing
coverage/
